# ── 运行数据（首次运行自动生成，不入库） ──
data/
runtime_logs/

# ── Python ──
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
*.egg-info/
build/
dist/

# ── 虚拟环境 ──
.venv/
venv/
env/

# ── 本地临时与日志 ──
temp/
*.log

# ── 编辑器 / 系统 ──
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── 本地私有配置（含真实密钥，禁止入库） ──
config.local.json
*.secret.json

# ── 开发备份文件（禁止入库） ──
*.bak
*.backup
*.bak_*
*.old_*
*.pre_ui*
*.before_*
*.wetware_backup
