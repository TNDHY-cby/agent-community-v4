# Agent Community v4 — 运行时依赖
# 安装：pip install -r requirements.txt
# 环境：Python >= 3.10

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
httpx>=0.27
pydantic>=2.5
click>=8.1

# 可选依赖：桌面 GUI 启动器（agent_community/gui.py）
# 不安装也能正常使用 CLI 与 Web 服务；Windows 上安装需 WebView2 运行时。
# pip install pywebview
